{
  "name": "paper-tetris",
  "version": "1.0.0",
  "private": true,
  "description": "Tetris estático con diseño Paper Mario. Sin build, sin servidor.",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "happy-dom": "^17.4.4"
  }
}